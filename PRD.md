# MomCare 产品需求：两个人一起照顾这段孕期

更新：2026-09-19。本文描述目标设计，不代表功能已经实现。

协作分工：ZCode 负责代码实现；Codex 负责方案设计和代码 review。本文修改不代表启动编码。

本文确定产品范围与共享规则；[DESIGN.md](DESIGN.md)定义页面与交互；[实施规划](DEVELOPMENT_PLAN_6_FEATURES.md)定义开发顺序和验收。旧版 docs/superpowers 中的登录、产检方案作为历史参考，冲突时以这三份文档为准。当前代码问题见[研究报告](docs/research/2026-09-19-review.md)。

## 1. 产品定位与范围

仅供开发者本人和妻子使用的微信小程序。一份共同孕期档案，妈妈和爸爸分别有符合日常关注点的首页；记录、产检、报告、待产准备共用同一套数据。

产品需要回答两个问题：

- 妈妈：“我和宝宝今天怎么样？有什么想记录的？今天希望他帮什么？”
- 爸爸：“她主动分享了什么？接下来有什么安排？今天我能完成什么？”

角色决定首页默认内容，不限制谁能操作工具，也不预设妈妈必须记录、爸爸必须采购。不做评分、打卡排名、连续记录压力或催促对方填写身体状态。

第一版不建设公开注册、多家庭运营、邀请码、VIP、付费会员、社区和商业推荐；不做复杂协同平台。六项工具保留为后续能力，不阻塞两人日常使用。

## 2. 身份、档案与同步

### 2.1 固定两位成员

- 计划通过微信小程序体验版供两人使用；真实 AppID、体验成员和云环境在交付时配置验证。
- 服务端配置一个固定家庭及两位允许访问的微信 OpenID，分别关联妈妈、爸爸身份；使用服务端可信的微信登录上下文识别成员。
- 不让前端提交的 OpenID、家庭 ID 或角色声明成为授权依据；第三个身份不能读取数据或加入家庭。
- 已绑定成员无需每次选择角色；“我的”可切换默认首页视角，但不能借此变更本人身份、代替对方操作或读取对方私人内容。
- 首次建档由任一成员发起，另一位直接读取同一档案，不再创建第二份孕期。
- 正式档案从空数据开始，由用户填写真实预产期等信息。演示模式单独存储、明显标注，不能自动导入正式档案。

### 2.2 轻量共享架构

已于 2026-09-19 确定采用微信云开发 CloudBase 云函数、文档数据库与文件存储，不购买独立云服务器。客户端本地缓存用于离线查看和暂存。纯本地存储不承担跨手机同步；迁移完成后移除正式业务对旧 Cloudflare 接口的依赖。接入、权限、数据迁移与交付要求见 [CloudBase 部署与迁移方案](docs/CLOUDBASE_PLAN.md)。此决定不表示迁移已完成。

第一版采用启动/回到前台、写入成功后和手动刷新拉取共享数据，不承诺后台实时推送。展示最近同步时间及“待同步 / 同步失败”。费用按实际云资源和 AI 用量控制，不作终身免费保证。

同条数据发生两端冲突时保留本地待提交内容，提示云端已有修改，让用户对比后决定；不做静默覆盖。退出或更换身份后清空当前会话及其本地缓存，私人内容不能跨身份加载。身份重新确认失败时不进入其他人的缓存。

首版冷启动先联网确认身份再展示持久缓存；已经验证的同一运行会话可离线查看和暂存。冷启动断网时保留草稿并提示联网恢复，不能用上一会话身份直接展示私人内容。这一取舍优先避免换微信账号时串用缓存。

## 3. 信息架构

沿用四个 Tab，路由尽量复用现有页面：

| Tab | 妈妈默认关注 | 爸爸默认关注 |
| --- | --- | --- |
| 首页 | 今天的状态、快捷记录、表达需要、下一次产检 | 主动分享的状态、我负责的事项、陪诊准备 |
| 知识 | 当周变化、记录方法、想了解的问题 | 当周陪伴方式、采购和日常照顾知识 |
| 档案 | 报告原件、解释、指标和待问医生的问题 | 同一份报告、共同关注的问题和待办 |
| 我的 | 个人偏好、孕期资料、私人记录、共享和数据设置 | 同样入口，显示本人身份与默认视角 |

共同任务完整列表从首页进入；常用工具从首页入口或“我的 → 全部工具”进入。爸爸视角属于首页，不再设计为工具箱里一个孤立的“爸爸模式”页面。

## 4. 两种首页

### 4.1 妈妈首页：今天的我和宝宝

默认顺序：

1. **孕期概览**：孕周、预产期倒计时、宝宝昵称；未建档显示“填写孕期资料”。
2. **今天的记录**：体重、血压、胎动记录入口；一键表达当前状态。没有记录时显示“今天还没记录”，不生成样例或正常结论。
3. **今天希望你帮什么**：自愿填写短句，例如“今天有点累，希望你下班带饭”；点击“分享给对方”后才成为共享需求。
4. **下一次产检**：日期、地点、要准备的材料、待问医生的问题，进入共同详情。
5. **共同准备**：今天相关任务、待产包未完成项。
6. **本周知识与常用工具**：提供少量相关内容，全部工具可展开。

快捷记录打开原记录表单；保存后可明确看到这条记录的共享范围。心情、症状叙述和私人备注默认仅本人可见，主动分享的状态卡可与私人日记分开写。

### 4.2 爸爸首页：今天可以为你们做什么

默认顺序：

1. **共同孕期概览**：相同的孕周与倒计时，显示共享数据的更新时间。
2. **她主动分享的状态与需要**：带日期和原文，不通过血压、体重或未打卡推断她的情绪。当天未分享时显示“今天还没有新的分享”。
3. **我负责的事**：按约定日期显示待办，可接下、完成或修改安排。
4. **下一次产检准备**：陪同安排、地点、材料、共同提问清单。
5. **今天可以了解的一件事**：有来源的当周知识，标注为一般建议，不表述成她正在出现的症状。
6. **共同准备与常用工具**：待产包、采购事项及全部记录工具。

爸爸同样能记录体重/血压、上传报告、操作计时器；代妈妈记录时明确显示“为妈妈记录 · 记录人：爸爸”，避免把爸爸本人的健康数据误写入共同孕期。

### 4.3 共同流程

**分享需要 → 具体行动**

妈妈写“下班带饭” → 明确分享 → 爸爸首页看到原文 → 点击“接下这件事” → 生成关联任务、负责人为爸爸 → 完成后两人看到完成状态。

- 一条需求可关联一项任务；重复点击、网络重试不重复创建。
- 需求作者可修改、结束或撤回共享；超过日期仍未结束的需求进入历史，不冒充今日状态。
- 撤回后从对方视图、关联任务及其后续缓存响应中移除需求内容；任务可保留无敏感原文的结束记录。对方已经读到的内容无法撤回记忆，离线缓存待其重新联网时清除。
- 对方只能接下行动或更新任务状态，不能修改作者表达的感受；取消任务不等于修改对方身体状态。

**产检 → 陪同与准备**

任一成员更新共同产检安排 → 选择陪同人、补材料和问题 → 双方首页展示各自重点 → 检查后上传报告 → 手动把需跟进事项加入任务。AI 结果不自动创建服药、就医等行动。

## 5. 数据共享边界

建档时用一句话说明：“这是你们共同的孕期档案，体重、血压、产检和报告会共享；心情和私人备注默认只有本人可见。”

| 数据 | 默认可见范围 | 修改规则 |
| --- | --- | --- |
| 孕周、预产期、医院、宝宝昵称 | 两人共享 | 两人可修改；记录操作者和更新时间 |
| 体重、血压、胎动等结构化记录 | 两人共享 | 两人可记录和纠错，注明受记录者与记录人 |
| 心情、症状叙述、私人备注 | 仅作者 | 仅作者读写；可另写一张共享状态卡 |
| 主动分享的状态与需要 | 两人共享 | 作者编辑、结束和撤回；对方可接下关联任务 |
| 产检、报告、提问清单 | 两人共享 | 两人维护；原件删除需确认 |
| 任务与待产包 | 两人共享 | 可指定任一成员或暂不指定；显示最后操作人 |
| 个人设置、私人记录 | 仅本人 | 切换首页视角不改变此范围 |

私人叙述与共享记录分开存储和返回，不能下载给对方后仅在页面隐藏。导出默认只包含共享数据，勾选“包含我的私人记录”才能加入导出者本人的私人内容。

“仅本人可见”限定于小程序及业务接口；云控制台/服务端管理员仍具备后台访问权限。首版没有端到端加密，首次使用私人记录时应说明该边界。

共享报告本身可能包含个人信息；上传前说明将进入共同档案。第一版不另做私人报告夹。取消共享不代表能让已导出的副本消失。

## 6. 共同能力

### 6.1 记录、日程与任务

- 孕周在跨日、回前台时更新。修改预产期时提示调整未完成产检安排，保留历史完成记录与手动约定日期。
- 记录支持修改、删除与持久恢复；保存失败不能显示成功。未知、未记录和未同步应区分。
- 产检包含日期/时间、地点、项目、陪同人、准备材料和提问清单。第一版只做应用内提醒，微信订阅消息后续单独验收。
- 任务包含标题、可选日期、负责人、来源和状态；支持待办、进行中、已完成、已取消。未指定负责人的任务可由任一人接下。
- 两人可调整负责人和安排；界面显示是谁做的调整，不设排行榜或“模范爸爸”评分。
- 待产包支持已备/未备、数量、存放位置及负责人，采购任务与原清单关联，避免生成两份重复进度。

### 6.2 报告与 AI

- 第一版优先验证图片上传、原件查看、归档和共同提问清单；PDF 为后续扩展。
- 同一报告原件与解读结果两人一致。妈妈视角默认展开指标解释；爸爸视角优先展示共同提问和准备事项，完整指标始终可展开。
- AI 解读保留原文、来源图和不确定状态；成功结果通过结构校验后再显示完成，失败不生成“正常”结论。
- 不设会员权益。两人共用必要的 AI 用量/费用上限；同一次任务重试不得重复扣次数。不可用时仍能保存与查看原件。
- AI 内容明确标注“仅供参考，具体情况请咨询产科医生”；共同任务由用户确认生成。

### 6.3 知识与工具

- 知识共用同一内容库，按当前孕周与所选视角改变排序；显示实际来源与更新时间，未审校内容不展示医生认证。
- 饮食速查、估重、胎动计时、宫缩计时分阶段实施，两个成员均可使用。
- 保留原始输入和记录，不依据未经确认的单一阈值给出安全保证；具体医学规则在功能实施前确认来源与适用边界。
- 产后状态由用户确认出生日期后切换；预产期到达不自动认定已分娩，产后内容扩展不属于第一版。

## 7. 第一版范围与验收

第一版交付：两个成员真实身份、一个孕期档案、两个首页、共享健康记录和产检、主动分享需要、共同任务、待产包、报告原件、基本导出与恢复。已有 AI 解读修复并完成真实联调后启用，否则保留明确的未启用状态。

暂缓：新医学计算器、500 词条规模目标、AI 自动兜底、PDF、外部消息推送、自动产后内容。首页工具入口可以先承载已可用功能。

必须通过以下使用场景：

1. 两台手机打开同一档案，首页重点不同；未允许的第三个身份无法读取任何内容。
2. 妈妈写私人备注，爸爸的接口响应、缓存、导出均不包含它；切换视角也读不到。
3. 妈妈分享需要，爸爸接下并完成，两端刷新后状态一致；重复点击不重复建任务。
4. 爸爸代记一条数据，两端能看到记录人；双方同改时提示冲突并保留本地输入。
5. 已验证会话断网保存明确标记待同步；重启后内容仍保留，联网确认同一身份后恢复，不丢数据、不重复写入。待上传图片不能仅引用临时文件。
6. AI 失败不显示成功、不把空结果标成正常、不扣成功次数。
7. 导出后可在隔离环境恢复；退出不会让下一个身份读取上一人的缓存。
8. 两人真机验证建档、记录、产检、分享、任务、报告、待产包全流程。

# MomCare 交付状态与后续顺序

更新：2026-09-20。用户指示：“你先把未部署这个记录下来，接着做后面的任务，等所有任务都做完后再处理。” 本记录覆盖此前“真实联调通过才继续 B2”的顺序安排。

## 执行约定

- **尚未部署。部署、真实配置、平台操作及双手机联调统一留到全部开发完成后处理。** 现在不要求 AppID/env，不创建或购买云资源，不调用真实供应商，不操作真实健康数据。
- ZCode 编写业务实现与回归；Codex 负责设计、独立 review、复现与验证。发现问题交回 ZCode 修复；阶段通过后按已有授权提交并推送 `origin`。
- “本地已验收”与“已部署/真机已验收”分开记录。缺外部环境不阻塞实现，但不可勾选真实云验收。
- 范围沿用 PRD 的第一版：B/C 业务与 D 交付准备。暂缓的新医学计算器、500 词条目标、PDF、外部推送等不因“所有任务”自动扩围。

## 当前状态

| 阶段 | 本地状态 | 提交 | 真实环境 |
| --- | --- | --- | --- |
| A 数据可靠性 | review 通过 | `faa5d19` | 未涉及部署 |
| B1 云基础与最小页面 | review 通过；52 项 B1、72 项历史 A、双端构建 | `b97b407` | 尚未部署，上传默认关闭 |
| B2a 同步与健康档案接入 | R5 本地 review 通过；42 项 B2a、42 项 B1（历史编辑器跳过）、72 项 A、19 份独立探针、双端构建 | `fa83f76` | 延后 |
| B2b1 产检、待产包 | 本地 review 通过；53项B2b1、156项历史回归、12组独立检查、双端构建与冻结哈希 | `9a473a5` | 延后 |
| B2b2 报告原件与附件 | 本地review通过；33组B2b2、209组历史回归、58独立入口、双端构建与冻结哈希；运行时异常与重跑记录见review | `e0cf22f` | 延后 |
| B3 迁移、完整导出与隔离恢复 | B3a 已验收；B3b 阶段一 codec/完整导出与本地验包本地 review 通过（368 项十套回归、80 项独立探针、双端构建）；阶段二隔离恢复待实施；不处理真实旧数据 | B3a `c4e5995`；阶段一见 Git 历史 | 延后 |
| C 双首页、分享需要与共同任务 | 待实施 | — | 延后 |
| D 本地发布准备 | 待实施；资源、包体、平台路径核对 | — | 双手机验收延后 |

每个子阶段都要求可运行生产路径、针对性测试与源码冻结证据；不以服务文件存在代替页面接入，不一次堆积全部阶段后才 review。

B2a 最终验收证据与历史修复见 [独立 review](PHASE_B2A_REVIEW_2026-09-19.md)。后续边界见 [剩余阶段验收](REMAINING_PHASE_ACCEPTANCE.md)。B2b1 已完成[独立验收](PHASE_B2B1_REVIEW_2026-09-19.md)；B2b2 已在 `e0cf22f` 验收。B3a 的冻结证据见 [B3 review](PHASE_B3_REVIEW_2026-09-19.md)，B3b 阶段一见[本轮独立 review](PHASE_B3B_DESIGN_REVIEW_2026-09-20.md)。B3b 阶段二、C、D 的方案不代表功能已实现。

## 六大功能核对

详见 [当前源码核对](SIX_FEATURES_IMPLEMENTATION_REVIEW_2026-09-19.md)：六项均未完整交付；专用速查/AI、B超估重计算未实现，胎动仅手动记录，宫缩仅有联系信息基础字段，家庭协同及统一首页提示仍缺阶段 C。A/B 基础阶段通过不等于六项完成。

## 开发完成后统一处理的未部署清单

1. 真实小程序 AppID、关联 CloudBase 环境 ID、两位体验成员与云端 OpenID 白名单。
2. 云函数部署、集合/索引与默认拒绝规则；本人暂存目录规则的控制台和实际拒绝用例验证，通过后才开启上传。
3. 真云 SDK 行为、事务冲突/重试、分页、删除与文件中断恢复；不以本地模拟结果替代。
4. 两台手机完整建档、共享/私人、记录、任务、产检、待产包、报告流程，以及第三身份拒绝。
5. 相机/相册、持久文件恢复、退出/切后台、冷启动断网、Wi-Fi/移动网络及真实小程序包体。
6. 真实旧数据由用户确认来源后执行迁移；真实备份在隔离环境恢复并打开附件，原数据保留。
7. OCR/DeepSeek 密钥和服务配置、第三方处理说明及真实成功/失败/超时验证；未验证前保持明确未启用，基本记录不依赖 AI。

部署时再核对平台与供应商的当前接口、限制、费用和手续。本阶段不承诺线上可用或真实 AI 已接入。

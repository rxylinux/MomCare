# MomCare 界面与交互设计：两个人，一段共同孕期

更新：2026-09-19。目标设计，待逐步实现。范围与权限以 [PRD](PRD.md) 为准，实施顺序见[规划](DEVELOPMENT_PLAN_6_FEATURES.md)。

协作分工：ZCode 按本文实现页面与交互；Codex 维护方案并对实现进行代码 review，不代替 ZCode 编码。

## 1. 设计方向

延续已有暖色卡片、圆角、留白和大数字，重点改造首页的信息顺序和操作入口。爸爸和妈妈使用同一套组件及品牌色，不按性别另做两套皮肤。

妈妈首页帮助记录和表达，爸爸首页帮助理解已分享的信息并完成具体行动。所有功能入口对两位成员可用，角色差异体现为默认排序与展开内容。

文案直接、亲近、不评判。使用“今天想记点什么”“今天希望你帮什么”“我负责的事”，避免“模范爸爸”“任务不达标”“她没打卡，快催一下”。

## 2. 导航与视角

底部沿用：首页 / 知识 / 档案 / 我的。任务列表从首页“查看全部”进入；全部工具从首页和“我的”进入，复用既有页面。

首页顶部显示“妈妈视角”或“爸爸视角”。首次按本人已绑定身份选择默认视角；顶部入口及“我的 → 默认首页视角”均可切换。切换保留当前登录身份和权限。

切换后的状态卡文案按真实作者和当前成员生成：本人写的显示“我分享的状态”，对方写的显示“对方分享的状态”。不会因为进入妈妈视角就获得妈妈的私人记录。代记录页面始终注明记录对象和真实操作者。

## 3. 首页结构示意

下方为内容顺序示意，不代表所有卡片必须塞进首屏。窄屏、长文案与大字体允许自然纵向滚动。

| 顺序 | 妈妈首页 | 爸爸首页 |
| --- | --- | --- |
| 1 | 孕周、倒计时、宝宝昵称 | 共同孕周、倒计时、同步时间 |
| 2 | 今天的记录：体重 / 血压 / 胎动 / 状态 | 对方主动分享的状态与需要 |
| 3 | 今天希望你帮什么 | 我负责的事：优先显示今天到期的前三项 |
| 4 | 下一次产检：项目与想问的问题 | 下一次产检：陪同、材料和行程 |
| 5 | 共同准备：任务和待产包 | 共同准备：待产包和未分配事项 |
| 6 | 本周知识、常用工具 | 本周陪伴知识、常用工具 |

两侧的产检卡打开同一详情页。完整体重/血压等记录在两侧均可访问；爸爸首页不堆满指标图表，但也不隐藏数据。

已过期未完成的任务与产检显示真实日期，不能混成今天新发生的事项。没有相关内容时使用简短空状态，不制造样例填满卡片。

## 4. 关键组件与交互

### 4.1 共同孕期概览

复用 HomeHero 和 DueCountdownRing 的展示基础，增加视角入口与同步提示。妈妈端数字以孕周/倒计时为主；爸爸端为状态与行动卡留出首屏空间。

- 未建档：“先填写孕期资料”，任一成员均可操作。
- 等待另一端数据：“正在加载共同档案”，不自动生成孕 16 周。
- 没有完成同步时显示缓存更新时间。
- 超过预产期仍显示真实状态，不自动切换为已出生。

### 4.2 快捷记录

复用 RecordEditSheet，健康数字与私人文字使用不同表单区和存储边界。

- 结构化健康记录顶部显示“记录妈妈的体重 · 两人共享”；爸爸代记时增加“记录人：爸爸”。
- 私人心情/备注显示“仅自己可见”，不与共享血压等字段一起提交。
- 首次使用私人记录时说明“仅自己可见”指小程序内访问权限，云服务管理员具备后台访问权限；不表述为端到端加密。
- 若希望告知对方，使用独立“分享状态”入口，打开待分享文本供用户确认。
- 正在保存时禁用重复提交；失败保留输入；离线写入本地成功后提示“已暂存，联网后同步”，不能显示云端已保存。
- 删除显示具体记录日期和内容；取消删除不改变数据。

### 4.3 分享状态与需要

建议组件：SharedStatusCard、NeedComposer。状态卡显示作者、日期、共享短句、关联行动；不自动附加私人备注或医疗数据。

**填写交互**

1. 可选快捷短句：“想休息一下”“希望帮忙带饭”“想一起准备产检”。
2. 用户编辑自己的文字，可选约定日期。
3. 提交前明确显示“对方可见”，按钮为“分享给对方”。
4. 保存后作者看到“已分享”，对方在前台刷新时可看到；不暗示消息已经推送或对方已读。

**接下交互**

对方点击“接下这件事”，生成一项关联任务，按钮变成“查看任务”。已有任务不再创建。完成后双方看到“已完成 · 完成人 · 时间”，原始分享保留为作者表达，不自动改写。

作者可以结束或撤回；撤回说明“联网同步后会从对方页面移除，已经看过或导出的内容无法收回”。关联任务不继续显示撤回的原文。

无新分享时写“今天还没有新的分享”，不写“她今天状态很好”。私人内容为空时不出现要求补全的提醒。

### 4.4 共同任务

建议组件：TaskListCard、TaskEditor、TaskRow；独立列表页建议为 pages/tasks/index.vue。

每行显示标题、约定日期、负责人和状态。支持接下、开始、完成、撤销完成或取消；重要操作保留操作者和时间，不建设复杂审批。

新建时标题必填，日期可空，负责人可选自己、对方或待分配。指派对方后清楚显示负责人，不自动声称对方已接受。双方均可调整安排。

任务从产检、待产包或共享需要产生时保留来源链接。撤销完成与重复同步都更新同一项；修改清单项目不会再复制一项采购任务。

### 4.5 产检卡与详情

建议复用现有 checkup-reminder 页并补齐共同准备区：

- 公共信息：日期/时间、医院、科室、项目、状态。
- 妈妈默认展开：检查事项、想问医生的问题。
- 爸爸默认展开：陪同安排、材料和行程准备。
- 同页可切换展开区，两人拥有相同维护能力。
- 应用内倒计时与“微信通知”区分。第一版不展示“已开启推送”开关。

### 4.6 报告、知识与工具入口

报告列表及原件共用。默认展开顺序随视角调整，不生成两份可能矛盾的 AI 结论。缺字段或识别失败显示“未识别/待确认”，不用绿色勾代表正常。

知识卡显示真实来源和更新日期。一般孕周知识使用“这一阶段可以了解……”的文案，不写“她现在正在……”。

首页常用工具最多一行四个，按使用阶段和个人偏好排序；“全部工具”显示完整可用能力。第一版复用记录、产检、待产包、报告入口；估重和宫缩等完成验收后再开放，不给尚未实现的入口伪造可用状态。“爸爸模式”从工具箱删除。

## 5. 共享状态与异常界面

| 状态 | 页面表现 | 可执行操作 |
| --- | --- | --- |
| 云端已保存 | 轻量显示更新时间 | 继续操作 |
| 本地暂存、待同步 | 黄色文字标记，不代表健康异常 | 查看待同步项 |
| 同步失败 | 保留内容与重试入口 | 重试、导出本地待同步数据 |
| 对方已修改同一条 | 显示云端与本地差异 | 采用云端或确认提交本地版本 |
| 内容被撤回/删除 | 下次同步后移除正文与相关缓存 | 返回列表 |
| AI 未启用/失败 | 原件可读，解释区说明实际状态 | 稍后重试 |
| AI 超时、结果待确认 | 保留原件及任务状态，不显示正常或完成 | 查询状态；再次调用可能计费时明确提示 |
| 冷启动离线、身份未确认 | “联网确认身份后恢复记录”，不显示原会话正文 | 联网重试；后台保留未同步草稿 |
| 未允许的成员 | 简单显示“仅限本家庭成员使用” | 返回，不展示家庭信息 |

同步提示不使用与医学危险提示相同的强红色。错误不能只靠 toast 一闪而过；未保存内容在当前页面有持续提示。

## 6. 视觉规范

### 6.1 色彩与层次

沿用现有品牌色 #C2185B、深色 #9B0044、浅色容器 #FCE7F3；主文字 #191C1E。暖白页面底色可延用已有 #FBF7F2，卡片用白色；后续实现统一到样式变量，不新增性别专属配色。

信息分组优先用留白和背景层次，密集列表可使用浅分隔线。可读性优先于绝对的“无线条”规则。颜色与文字/图标共同表达状态，不用红绿颜色独自判断含义。

### 6.2 排版与触控

使用系统中文字体，避免依赖外部字体下载。正文建议 15–16 px、行距约 1.6；辅助文字保证可读。孕周等关键数字可用 36–48 px，但应让位于需要操作的按钮。

可点击区域按至少 44 × 44 px 的设计目标布局；保留现有 16/24/32 px 间距和 16/24 px 卡片圆角。使用 rpx 时按实际屏幕校验，不强制每屏固定留白比例。

### 6.3 动效与跨端

保留轻量展开和状态切换，减少装饰动画。计时、保存状态不依赖动画或前台 setInterval 累加；回前台后显示真实经过时间。iOS/H5 专属安装入口不出现在微信小程序。

长任务名、医院名称、共享短句支持换行；底部操作区避开安全区和微信胶囊，键盘弹起时提交按钮可见。

## 7. 页面验收示例

- 妈妈上午写了私人心情、下午单独分享“帮我带饭”：爸爸只能看到下午主动分享的短句。
- 爸爸接下后断网再次点击：只有一项任务，显示待同步，重启不丢失。
- 爸爸切换妈妈视角：仍显示爸爸身份，只改变排序，不能看到妈妈私人心情。
- 昨日分享今天仍在历史里：爸爸首页不把它显示为今天的新状态。
- 两人打开同一报告：原件与解读内容相同，只是默认展开区不同。
- 无健康记录、无分享、无任务：页面可用，有自然空状态，不填入演示健康信息。

界面检查覆盖两位成员、两种首页偏好、空数据、长文本、离线、失败、大字体和真机安全区。

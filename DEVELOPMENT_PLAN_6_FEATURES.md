# MomCare 实施交接：两人共享与六项功能

更新：2026-09-19。此文档供 ZCode 实现，Codex 负责方案设计和代码 review。

产品约束见 [PRD](PRD.md)，页面交互见 [DESIGN.md](DESIGN.md)，已观察到的旧实现缺陷见[研究报告](docs/research/2026-09-19-review.md)。本文所有新增文件/组件名称都是建议落点，不代表已存在或已完成。

部署选型已确定为微信云开发 CloudBase，阶段 B/D 须同时满足 [CloudBase 部署与迁移方案](docs/CLOUDBASE_PLAN.md)。不再新增 Cloudflare 正式业务依赖；ZCode 负责迁移实现，Codex 评审代码和验证证据。

## 1. 协作分工与实现边界

| 参与方 | 职责 | 交付物 |
| --- | --- | --- |
| 用户 | 确认实际使用偏好、提供必要平台配置、体验两台手机效果 | 体验反馈与必要的平台操作 |
| Codex | 产品与技术方案、文档一致性、实现后的独立代码 review | 方案文档、按严重程度排序的问题与复现证据 |
| ZCode | 修改业务代码、数据迁移、必要测试、构建和联调 | 实现差异、验证结果、已知限制与交接说明 |

Codex 不代替 ZCode 编码或直接修复 review 问题；可在 review 时阅读 diff、运行已有验证及检查行为，将发现交回 ZCode 修复。只有用户明确改变分工，才调整此边界。

每阶段交接提供修改文件、提交号或当时工作区状态、验证命令及结果、未验证场景；Codex 据此评审。ZCode 的“构建成功”不能替代业务场景证据。发现问题后围绕问题做针对性复验，稳定阶段再做双端整体验证，无需每次小修改重跑全部流程。

阶段提交规则（用户于 2026-09-19 授权）：每个阶段由 ZCode 编码，Codex 独立 review；发现阻塞问题先交回修复。review 通过且必要验证完成后，Codex 可直接为该阶段创建 Git 提交并推送到本项目 origin，无需重复请求确认。提交前核对验收源码与实际差异，记录验证范围、未完成事项和提交结果。Git 提交与推送不等于部署或上线验收。

执行顺序补充（2026-09-19 用户最新指示）：**尚未部署先记录，继续完成全部后续开发，再统一处理部署、配置和双手机联调。** AppID/env、成员配置及供应商密钥暂不要求用户提供；不阻塞本地可完成的 B2/B3/C/D 准备工作。阶段仍按本地代码与测试独立 review 后提交，真实环境验收列为最终待办。状态以 [DELIVERY_STATUS](docs/DELIVERY_STATUS.md) 为准。

本项目只服务一个固定家庭的两个人。不要扩展多租户平台、邀请码系统、角色权限后台、会员或任务积分，也不要重写整套 UI。

## 2. 本轮设计的关键变化

- 两种首页是产品主结构；删除“爸爸模式是一个独立工具页”的设计。
- 首版优先共同档案、共享需要、任务和可靠数据流；工具箱入口先接已有功能。
- 妈妈默认关注记录与表达，爸爸默认关注已分享状态和自己负责的行动；所有工具对两人开放。
- 私人叙述独立存储，默认不共享。视角切换仅是偏好，不能改变服务端身份和访问范围。
- 保留原六项功能意图，但将家庭协同和入口前置，将医学计算与 AI 扩展安排到后期。
- 去掉终身免费、自动权威认证、统一胎动正常阈值和“加上免责声明即完成安全要求”等未验证承诺。

## 3. 最小数据约定

只设一个固定家庭和一份当前孕期档案，不需要通用家庭创建系统。服务端可信身份映射到两个成员；家庭常量不是访问凭证。

以下为逻辑实体，ZCode 可按实际 CloudBase 存储组织集合，但须保留可见性边界：

| 实体 | 最少信息 | 访问与含义 |
| --- | --- | --- |
| 成员配置 | memberId、可信 OpenID 映射、身份、默认视角 | 服务端验证准入；前端不能改身份映射 |
| 孕期档案 | pregnancyId、预产期、宝宝昵称、医院 | 两人共享；首次建档只创建一份 |
| 健康记录 | id、pregnancyId、日期、指标和值、subjectId、recordedBy | 指标记录妈妈的健康信息；注明实际操作者 |
| 私人文字 | id、ownerId、日期、内容 | 仅作者可读写；与共享健康字段分离 |
| 共享需要 | id、authorId、目标日期、文本、active/closed/withdrawn | 作者控制原文；显式分享后才对方可见 |
| 共同任务 | id、标题、日期、负责人、来源 ID、状态 | 两人维护；pending/doing/done/cancelled |
| 产检日程 | id、日期时间、地点、项目、陪同人、材料、问题 | 两人共享；保留手动安排和已完成历史 |
| 报告 | id、文件引用、日期、类型、上传者、AI 状态 | 两人共享；原件和 AI 结果分离管理 |
| 待产包项目 | id、名称、数量、存放位置、负责人、准备状态 | 关联任务按同一项目更新，不重复记完成进度 |

可变云记录带 revision、updatedAt、updatedBy；本地操作带稳定 operationId 和待同步状态。仅为防止重复和静默覆盖，不引入通用事件总线或完整事件溯源系统。

### 3.1 共享与私人规则

- 云端先校验当前身份属于这两人，再校验私人记录 ownerId；不信任客户端传入的 ownerId。
- 列表、详情、搜索、导出和 AI 上下文均应用相同范围。私人文字默认不传给 AI。
- 分享需要复制的是用户明确确认的短句，不自动附带原私人日记全文。
- 撤回共享后返回无正文的撤回状态以清除另一端缓存，关联任务不再携带原文；已导出副本和对方已经读到的内容不承诺收回。
- 共享导出默认不含私人内容；导出者可单独勾选加入自己的私人记录。
- 身份变化先清空原会话与缓存；不能把匿名/演示数据自动归并到已认证档案。

### 3.2 最小同步协议

1. 首次联网确认成员身份，读取当前共同档案及本人私有数据。
2. 写入先保留本地内容与稳定操作 ID；有网络时提交，收到服务端确认后标已同步。
3. 后续重试复用操作 ID；同条数据以 revision 检查冲突。冲突保留本地输入，提示采用云端或确认重提，不默认覆盖。
4. 两人共享数据量小，第一版可全量拉取云端快照，但必须完整分页、与本地待同步项按 revision 合并，并正确处理删除/撤回标记。部分页面成功不算全量成功，不根据列表缺失推断删除；具体约定见 CloudBase 方案 4.2。
5. 启动、回前台、提交成功后及手动刷新拉取更新；不做常驻后台实时连接。只显示真实最近同步时间。
6. 退出前有待同步内容时说明后果，允许先同步或导出本人可访问的待同步数据；用户确认丢弃后才清理。不得悄悄丢弃，也不得交给下一个身份。
7. 日期按家庭时区 Asia/Shanghai 处理；计时事件存绝对时间，回前台重新计算。日号比较、午夜刷新与日期迁移独立可测。
8. 冷启动联网确认身份后恢复持久缓存；运行中已验证的会话可离线暂存。操作重试复用相同 ID 与内容，解决冲突后的新提交使用新 ID；待上传附件须持久保存。

## 4. 页面和代码组织建议

保留现有 Vue/Pinia 技术栈与四个 Tab，职责拆分仅到实现所需程度。

| 落点 | ZCode 实现内容 |
| --- | --- |
| pages/index/index.vue | 根据本人默认视角组合首页，两端共用查询与操作 |
| components/home | 复用 Hero、日历、记录；新增共享状态、需求输入、任务摘要、产检准备组件 |
| pages/tasks/index.vue（新增） | 共同任务列表与编辑，承担原“爸爸模式”行动清单 |
| pages/profile/index.vue | 本人身份、默认视角、全部工具、共享说明、导出入口 |
| pages/profile/checkup-reminder.vue | 共同产检资料、陪同、材料、问题；保持应用内提醒 |
| pages/profile/hospital-bag.vue | 共享清单与任务关联、位置和负责人 |
| pages/archives | 统一上传/鉴权/状态，两个视角打开同一报告 |
| stores 与服务模块 | 分离身份、数据读写及首页计算；不再让 health Store 承担所有职责 |
| CloudBase 配置与云函数 | 固定两成员准入、共享/私人读写、文件访问和可选 AI 服务 |
| static/data 与加载器 | 小程序可用的内容加载；按视角排序同一库；保留来源元数据 |

建议用一个 authenticated member 状态和独立 homeView 偏好，不复用 guest_mock_token 表示真实登录；禁止根据 homeView 决定 private 数据访问。

## 5. 第一版交付顺序

### 阶段 A：消除现有数据错误

ZCode 实现：

- 纠正网络失败伪成功、上传 token 不一致、存储失败被吞掉。
- 隔离演示模式，真实档案空状态；禁止自动填入孕期与健康记录。
- 修复报告覆盖/归档列表、日期缓存、产检日期迁移。
- 清理假容量、假同步、假注销等展示；只展示真实可用动作。
- 对旧数据先做可恢复备份，提示用户选择保留或排除可能的演示记录，不能仅按数值等于样例就自动删除。
- 旧缓存没有可信所属成员时不自动上传或归属当前登录者；由用户确认来源后迁移。旧心情和备注默认进入确认后的作者私人区，不因迁移变成共享内容。

交给 Codex review 的证据：断网请求不成功、失败 AI 不扣次数、存储失败可见、本地记录重启可恢复、待同步报告不会被空云列表清除。

### 阶段 B：两人身份与共享数据

ZCode 实现：

- 配置真实 AppID 关联的微信云开发环境，通过原生云调用识别两个成员，建立一份孕期；不保留游客 token 作为正式身份。
- 将核心记录、产检、待产包、报告原件接入共同数据源；完成私人内容边界。
- 实现最小同步、冲突提示、退出清理、共享数据导出和恢复。
- 迁移验证完成后切断正式路径对原 Cloudflare 后端的依赖。
- 已有 AI 解读在成功校验、失败不扣次数及真实联调完成前保持明确的未启用状态。

交给 Codex review 的证据：两端交叉修改、非成员被拒绝、私有内容不进入对方响应/缓存/导出、退出不串数据、导出在隔离环境可恢复。

### 阶段 C：两种首页与具体行动

ZCode 实现：

- 按 DESIGN.md 完成两个首页和默认视角偏好。
- 实现主动分享需要、接下任务、完成/取消及撤回共享。
- 产检准备和待产包接入共同任务；入口包含已可用工具。
- 知识内容按视角改变推荐顺序，但不虚构她当前身体状态。

交给 Codex review 的证据：分享到任务完整流程、重复接下幂等、过期内容不冒充今日、切换视角不改变权限、两种视角都能代记录且显示实际操作者。

### 阶段 D：两台手机第一版验收

ZCode 完成构建和真机联调，用户完成必要平台操作与实际试用；Codex review 代码与验证结果：

- 替换 touristappid，压缩 logo、排除 H5/iOS 专属资源，核实真实平台包体要求。
- 验证小程序静态内容加载、导航、输入、相机/相册、文件恢复与后台切回。
- 两台手机走通建档、记录、分享、任务、产检、报告、待产包；无假成功和假同步。
- 使用体验版关闭调试且不开代理，分别在 Wi-Fi 和移动网络验证云调用、上传、查看原件和双方同步；禁止以开发工具跳过域名校验的结果代替真机验收。
- 明确列出尚未启用的 AI、新工具、PDF、外部推送，不把未验证项勾为完成。

阶段 A–D 构成第一版；之后按实际孕期需要选择扩展工具，不强制一次性做完。

## 6. 原六项功能的调整后规格

### 功能一：饮食/行为速查与可选 AI 解释

保留 pages/tools/food-safety.vue 的计划落点。先实现少量有可靠来源、更新时间和适用条件的高频词条及同义词搜索，再扩展覆盖；不以 500 条数量作为首版门槛。

本地确认词条优先。搜索未命中明确显示未知，用户主动点击后才发起 AI 解释；AI 缓存单独标“AI 生成，未审校”，不能覆盖已核实词条或升级成权威库。分类含“资料不足”，不强迫所有问题都落在安全/危险二选一中。

妈妈可查询日常疑问，爸爸可用于采购准备；双方用同一份解释。药物等问题不输出未经验证的具体用法或替代医嘱。

验收：离线可查已有词条；未知不会变为安全；缓存保留来源、时间和生成性质；失败不产生成功结果。

### 功能二：B 超估重记录与计算

保留 pages/tools/ultrasound-calc.vue 的计划落点。实施前确认公式版本、输入含义、单位和参考来源；此文档不把旧公式系数当作已经审定的算法合同。

支持输入检查日期、孕周与测量值，明确 mm/cm；保留原始值、计算版本和操作者。先交付“估算值 + 来源 + 待问医生的问题”，不直接给出生长异常诊断。没有经过确认的参考曲线时不展示百分位或正常区间。

验收：单位切换不改变物理量；边界输入可解释；固定样例与所选公式来源核对；历史结果可追溯，不因算法更新静默变化。

### 功能三：胎动记录与计时

复用 pages/profile/fetal-records.vue。原有手工记录保留，计时会话单独存开始/暂停/恢复/结束与原始点击事件；爸爸可协助操作，注明记录对象和操作者。

计时长度、连续动作合并方式及结果提示，依据适用指南和医生建议另行确认。取消旧版“统一五分钟合并、达到三次即正常”的既定验收要求，不用数字自动提供安全保证。原始点击保留，聚合方式可追溯。

验收：暂停、退后台、意外关闭后可恢复；误触能撤销；不把零记录、未完成会话当作正常结果。

### 功能四：宫缩计时与联系信息

保留 pages/tools/contraction.vue 的计划落点。先实现大按钮开始/结束、持续时间和间隔列表、医院联系方式；间隔定义为相邻两次开始时间差，持续时间为同次结束减开始。

保留原“5-1-1”作为待专业确认的规则候选，而非默认临床判断；未经确认不自动启用，计时结果不作为是否联系医院的唯一条件。父母均可操作，正在计时的会话明确显示操作者，避免两台设备把不同输入混成一个会话。

验收：连续点击防重复、误触修正、后台恢复、设备时间异常提示、未结束会话不进入完整时长统计；医院号码由用户确认保存。

### 功能五：家庭协同与爸爸首页

纳入第一版阶段 B/C，替代 pages/tools/father-mode.vue 独立工具页方案。核心交付是共享档案、爸爸首页、妈妈主动表达需要和双方共同任务。

爸爸知识建议与她的真实状态分开显示。没有分享不推断情绪，不用“模范”评价；爸爸可负责记录、妈妈也可负责采购。细节以 PRD 与 DESIGN 为准。

### 功能六：首页入口与统一结果提示

纳入第一版阶段 C，常用入口按视角/个人偏好排序，不强制固定五格。工具未验收就不显示可用按钮；爸爸视角作为首页偏好入口。

统一 AI/工具的来源、未知/失败、免责声明与同步状态组件。提示不能替代结果校验，不靠颜色单独表达风险，不把报告未识别渲染为“全部正常”。

## 7. ZCode 交接给 Codex 的最小清单

每阶段完成后提供一份简短交接：

1. 本阶段实现了哪些用户场景，哪些明确未实现。
2. 修改文件和提交号；若未提交，注明 review 对应的工作区状态及尚在变动的文件。
3. 数据字段/权限/迁移变化，以及回退时如何保留用户数据。
4. 已执行的针对性测试与结果；阶段收尾提供构建和对应真机验证。
5. 已知缺陷、外部配置依赖与未验证项。

Codex review 优先检查真实数据不被伪造或丢弃、两人共享与私人边界、首页角色语义、离线/重试/冲突和交接证据。输出可定位的问题和建议修复方向，交由 ZCode 修改；通过后再确认该阶段可继续。
